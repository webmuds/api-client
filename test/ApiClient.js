'use strict'

import { expect } from 'chai'

import ApiClient from '../index.js'

describe('ApiClient', function () {
  // This is just a placeholder, replace later
  it('loads', function () {
    expect(ApiClient).to.not.eq(undefined)
  })
})
